// import * as mammoth from 'mammoth';
//
// export async function readFile(
//   config: Parameters<typeof mammoth.extractRawText>[0],
// ): Promise<string> {
//   return (await mammoth.extractRawText(config)).value;
// }
